#ifndef DEFINES_H
#define DEFINES_H

#include <types.h>
#define MAP_WIDTH   10
#define MAP_HEIGHT  10
#define MAP_SIZE    (MAP_WIDTH * MAP_HEIGHT)

// defines the number of signatures the CPU player will store during a land auction
#define PLAYER_SIG_SIZE 4

#endif